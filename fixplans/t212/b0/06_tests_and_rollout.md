# 06 口径矩阵、规格修订、测试与上线（对话 T 主责，D 配合）

前置：`00_coordination.md` 全文。

## 1. 实施前的事实复核闸（硬性）

`01` 至 `05` 各自的「现状事实」表是本轮按代码逐条核过的，但代码会变。
**每个对话开工的第一个动作**：对本文件之外自己那几份计划的事实表逐条复核（打开被引用的文件确认），
把不符者就地更正并在 `00` §10 追加一行。未复核即动手属流程违规。

行号只作定位提示，不作契约；契约在 `00` §2、§3、§5。

## 2. 口径矩阵（复现判据的唯一定义处）

| 维度 | A1 研究臂 | A1 实盘臂 | B0 研究臂 | B0 实盘臂 |
|---|---|---|---|---|
| 引擎起点 | 2019-06-03 | 2019-06-03 | 2010-01-04 | 2010-01-04 |
| 评估窗 | 2020-01-02 至 2026-08-28 | 同左 | 同左 | 同左 |
| `live_from` / `rebalance_anchor` | 2020-01-02 | 2020-01-02 | 2020-01-02 | 2020-01-02 |
| 排名口径 | `rank_as_of == as_of` | `rank_as_of` 为前一场次 | 同研究 | 同实盘 |
| 场次序列 | SPY 全部场次（半日市计入） | 同左 | 同左 | 同左 |
| 准入条数 | 四条 | 五条（含 E5） | 四条 | 五条 |
| 目标顺序 | 排名序 | 排名序 | `sells_first = false`，集合按 `sorted()` 迭代 | `sells_first = true` |
| 费率档 | worst | worst | worst 与 actual 各一 | 同左 |
| 本金 | £10,000 | £10,000 | £1,000 | £1,000 |
| 期末判据 | **111,347.561961** | 本轮产出 | **17,469.4818**（actual **18,793.7032**） | 本轮产出 |

判据来源：`backtest/results/xsmom_a0_headtohead_causal_20260902.csv`、
`backtest/results/a0_a1_merge_summary_a1_20260902.csv`。规格正文只记四舍五入值，精确值以 CSV 为准。

实盘臂与研究臂的差异恰为**三项**：排名滞后（A3）、准入 E5（A5）、目标顺序（A6）。
逐项消融（一次只改一项）并把各自贡献写入新裁定；不得为凑近研究口径而调整任何参数。

## 3. 规格待修订清单（改规格在前，改代码在后）

| 文件 | 条目 | 改什么 |
|---|---|---|
| `a1_spec.md` §3.2 | 准入 | 增第五条 E5：无已验证 ticker 的候选不准入（`00` A5） |
| `a1_spec.md` §13.6 | 复现判据 | 精确值 111,347.561961，注明引擎起点 2019-06-03 与所属臂 |
| `b0_spec.md` §3.4 | 免动带 | 区分「无价格」（清零）与「行情延迟或排名表陈旧」（冻结，`02` D7） |
| `b0_spec.md` §9.4 | 复现判据 | 增实盘口径行；原值标注为参考实现口径，并注明引擎起点 2010-01-04 与迭代顺序前提 |
| `b0_spec.md` §9.2 | 注入 | 与 `00` §2.3 的注入包结构对齐 |

## 4. 测试矩阵

| 层 | 文件 | 来源 |
|---|---|---|
| 策略 | `tests/strategy/test_{a1,b0}_v0_0_1.py` | `01` §6、`02` §6 |
| 数据 | `tests/ingest/test_a1_rank.py`、`test_yahoo_bars_guard.py` | `03` §8 |
| 执行 | `tests/execution/test_b0_*.py` | `04` §11 |
| 看板 | `tests/dashboard/test_b0_*.py` | `05` §10 |
| 回归 | 现有 **330** 项收集（323 通过、7 跳过）全部不变 | 本回合 `pytest tests/ -q` 实测 |

新建目录（`tests/strategy/`、`tests/ingest/`）各配 `README.md`；`tests/execution/` 现无 `README.md`，一并补。

## 5. 提请用户裁定（一次提清，`CLAUDE.md` §1.4）

| # | 事项 | 建议 | 理由 |
|---|---|---|---|
| U1 | B0 账本分配额 | 等于现 A0 账本现金与持仓市值之和 | 一池打满的定义 |
| U2 | `max_daily_orders` | 80 | 重排日上限 58 加精度重试余量 |
| U3 | `max_order_notional_gbp` | 按**最大可能持仓价值**定（例如分配额除以 18 再乘 2），并同时采纳 `04` §6.3 的卖单豁免 | 不豁免则涨幅超限的持仓永远退不出，清零规则等于未实现 |
| U4 | `max_gross_notional_gbp` | 分配额乘 1.05 | 满仓加余量 |
| U5 | 重排日的提交提前量 | **撤销**早前「重排日 120 秒」的提议 | 代码只有单一全局 `submit_lead_sec`，改它会让 A0 的成交口径偏离回测 `close_window_sec = 60`（`close_gap_bps` 的标定前提），属口径变更而非风控限额。改用 `04` §6.4 的顺延规则。若仍要区分，须新增 `execution.submit_lead_sec_rebalance` 并改 `_Cycle`、`decide`、`daemon.plan_next` 与 `_notify_missed_sessions` 四处，同时把它列入 §2 的消融项 |
| U6 | `priority` | `"a1"` | 回测口径 |
| U7 | A0 账本迁移时点 | **迁移日即 B0 起用日，也是重排序号 0（重排日）** | 起用日按 `01` D4 必为重排日；若为避开重排而把锚点设早，A1 首日就不是重排日、名单无从建立 |
| U8 | 映射的多候选与未匹配名单 | 未裁定者不准入 | 保守 |
| U9 | B0 实盘采纳裁定 | 见 §6 | 流程要求 |

## 6. 采纳裁定（上线前必须存在）

`research/decisions/20260902_xsmom_wide_ruling.md` §8 把合并轮列为条件项，`b0_spec.md` 状态为「未裁定采纳」。
上线前须有：

1. 预注册 `research/prereg/2026XXXX_b0_live_prereg.md`：接受的风险画像（回撤约 45%）、演练通过标准（§7）、
   实盘首月的停止条件。
2. 演练后的裁定 `research/decisions/2026XXXX_b0_live_ruling.md`：采纳、不采纳或带条件采纳。
3. 只有采纳或带条件采纳才进入 §8。

## 7. 模拟盘演练（`CLAUDE.md` §3.3 硬性前置）

| 步 | 内容 | 通过标准 |
|---|---|---|
| 0 | 修复 paper 环境现存 abort（`04` 事实 15）：demo 账户可用现金不低于 B0 账本现金 | paper daemon 完成一次 decide 与 settle |
| 1 | 精度探针（`04` §7.5，**demo 下真实委托，须用户当轮授权**） | `qty_steps.json` 覆盖当期名单 |
| 2 | paper 环境 `dry_run = true` 连续 5 个场次：decide 不 abort、目标非空、三条记录流每日有行、看板可渲染 | 5 of 5 |
| 3 | paper 环境 `dry_run = false`（demo 真实下单）连续 10 个场次，含至少一次重排日 | 对账 10 of 10；无歧义冻结；拒单原因全部可解释；重排日订单在提交窗内完成或按顺延规则次日补齐 |
| 4 | 每场次核对看板与记录流一致（`05` §10 测试 8） | 差异不超过 £0.01 |
| 5 | 写 `research/decisions/2026XXXX_b0_paper_rehearsal.md`：逐场次结果、拒单表、时序实测 | 入档 |

第 3 步的连续多场次自动下单依据 2026-08-31 用户裁定（看板 `dry_run` 开关为唯一控制、daemon 每周期重读），
不适用 `CLAUDE.md` §3.1 的逐次授权；该裁定须在演练记录中引用。

## 8. 上线

| 步 | 内容 |
|---|---|
| 1 | **先把 live 的 `dry_run` 由 `false` 改为 `true`**（当前为 `false`，A0 已武装实盘；`04` 事实 14），须用户当轮授权，经看板开关 |
| 2 | 改 live 配置：`strategy.name` 与 `intraday_name` 为 `b0`、`b0_live_from`、U1 至 U4 与 U6 的值 |
| 3 | 迁移账本（`04` §10），须用户当轮授权；迁移后 `reconcile` 通过 |
| 4 | live 环境 `dry_run = true` 跑 3 个场次，确认决策与记录正常且无订单 |
| 5 | 用户当轮授权把 `dry_run` 改回 `false`；首个实弹场次结束后人工核对成交与账本 |
| 6 | 首月每周复盘：拒单、延迟、归因、与回测的偏差，写入 `WORKING_MEMORY.md` |

## 9. 回滚

| 层 | 动作 |
|---|---|
| 立即 | 看板紧急停止落 `halt` 旗；`dry_run` 改为 `true` |
| 账本 | `ledger_store.restore_ledger`（`04` §10.1）把 `.retired-<stamp>` 改回原名；B0 账本 retire |
| 配置 | `strategy.name` 与 `intraday_name` 改回 `a0` 与 `a0_intraday` |
| 数据层 | 守卫、第四 pass、锁的改动无需回滚，对 A0 无影响 |
