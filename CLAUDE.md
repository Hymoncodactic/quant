# 项目规则（always-on，不需调用即生效）

> 本文件是 **验证纪律**，对本项目内所有对话、所有任务无条件生效。
> 代码开发的**完整流程**另见 skill：`/verified-dev`。
>
> **本项目与 `WORK FILES/` 工作区完全独立**：两边的规则、记忆、skills、数据互不引用、
> 互不复制。本项目的私有记忆在 `~/.claude/projects/-Users-hb-Desktop-quant/memory/`，
> 与工作区记忆目录物理隔离。禁止把工作区的标的、口径、结论、路径带入本项目，
> 反之亦然。

---

## 零、项目定位（一句话）

个人自有资金的量化交易项目，两条并行方向：

| 方向 | 场所 | 标的范围 | 计价货币 |
|---|---|---|---|
| 加密货币 | OKX | 主流高流动性币种（BTC、ETH 等） | USDT 计价，见 `crypto_trading/config/` |
| 股票 | Trading 212 | 高流动性股票 | GBP（账户内存放的是英镑） |

两条线共用一套纪律（本文件）、一套 skills 与 `common/` 基础库，
但**数据、配置、风控参数、回测结论一律分场所隔离**，不得互相套用。

---

## 一、推断纪律（最高优先级）

### 1.1 什么算「已证实」

一个判断只有满足以下**任一条**，才可以当作事实使用：

| # | 证实来源 | 本项目的举例 |
|---|---|---|
| S1 | **本地代码**：能指出确切文件与行号 | `MAKER_FEE_BPS = 8`（`crypto_trading/config/fees.py` L12） |
| S2 | **本地生效配置**：实际被加载的那一份 | `crypto_trading/config/okx.live.yaml`（由 `QUANT_ENV` 决定加载哪份） |
| S3 | **本地说明文档**：且已与代码交叉比对无冲突 | `data/okx/DATA_SPEC.md` 的字段定义 ∧ 代码实际读取一致 |
| S4 | **权威外部源**：交易所/券商官方文档、官方接口返回值 | OKX API 官方文档的字段说明、`GET /api/v5/public/instruments` 的实际返回 |
| S5 | **数据实证**：可复现的量化检验，且该检验有判别力 | 用实际下载的 K 线验证时间戳是开盘时刻还是收盘时刻 |
| S6 | **用户在 prompt 中明言的要求或事实** | 「Trading212 里面我存放的是英镑」 |

**除此之外的一切，都是推断（inference），不是事实。**

**本项目的特别提醒**：交易所 API 的字段含义、单位、精度、费率、限频、
最小下单量——**一律不得凭模型记忆作答**。这些是 S4 类事实，必须现查官方文档
或现调接口取回实际返回值，并在 Reference 清单中给出文档 URL / 接口路径 / 返回样本。
凭记忆写出的 API 参数是本项目最高频的错误来源，且错误直接对应真金白银的损失。

### 1.2 遇到未证实的假设 —— **停下来问，不要静默推断**

只要某个判断达不到上面任一条，**必须停止，向用户提问**。以下三种做法一律禁止：

1. 默默按最可能的猜测继续做下去。
2. 把反推（用结果倒推输入）包装成「已校验」「已验证」。
3. 只在文档角落写一句免责，正文却当事实用。

提问时必须给出：**① 我的推断是什么 ② 依据是什么、强度如何 ③ 怎样才能真正证实
（具体到查哪个文件/哪个官方文档章节/哪个接口字段）④ 如果推断错了会有什么后果
⑤ 本方的推荐做法及其理由**（§1.5）。

### 1.3 用户 prompt 中的明言（S6）的特殊处理

- 用户明言的要求或事实**可直接当作已证实的假设采用**，不必反复求证。
- 若它与代码事实、文档记载或既有认知相矛盾，**必须停下来向用户核实**：
  - 不得默默按用户说的做（可能是口误或记错）。
  - 不得默默按代码事实做（会违背用户意图）。
  - 正确做法是摆出冲突双方的具体证据，请用户裁定。

### 1.4 关键决策归用户

以下情形一律先问，不自行拍板：

- 会**改变交付物中的数字**的口径变更
- 涉及**外部事实**（交易所规则、费率档位、账户实情、可用资金额度、税务口径）
- **写死在代码里的常量**，其取值依据不在本地
- 结论方向的**定性表述**（如「保守下界」vs「乐观上界」）
- **任何影响实盘下单行为的参数**（阈值、仓位上限、频率上限、止损位）

### 1.5 决策请示的形式（硬性）

用户不逐行读代码，也不承担本方本可自行判定的判断。凡向用户请示，一律按本条执行。

**第一原则：先自己扛，不要把工作反向丢给用户。**
凡同时满足「可逆」「本方能自行验证」「不触及 §3 资金红线与 §1.4 外部事实」三条的事项，
一律自行决定并执行，事后在输出中一句话告知处置与理由，不得列为待办抛给用户。
把可自行判定的事项包装成「需你裁定」，与静默推断同属违规，后果由本方承担。

**第二原则：凡请示必带推荐。**
确需用户裁定的事项，必须同时给出下列两者之一，缺失即视为提问无效：

| 形式 | 要求 |
|---|---|
| 推荐做法 | 明确写出本方建议怎么做，加一句理由。用户回一个字即可执行 |
| 备选项 | 每项标注代价、后果与可逆性，并指明本方推荐哪一项及理由 |

禁止只抛问题不给方案，禁止「你看怎么办」「需要你确认一下」这类无内容的请示。

**第三原则：控量与排序。**
一次交付中需用户裁定的事项超过 3 项时，按对结果的影响排序，只呈现必须由用户定的；
其余按推荐做法直接执行，并在输出中列一行「已按 X 处理，需改动告知」。

**第四原则：证据冲突照样带推荐。**
按 §1.3 摆出冲突双方证据时，仍须给出本方倾向哪一方及理由，不得只列证据不表态。

---

## 二、表述纪律

1. **区分「跑通」与「正确」**：程序无报错 ≠ 逻辑正确。二者必须分别陈述。
2. **数字必须带出处**：引用任何数值，注明来自哪个文件/行号/命令/接口返回。
3. **统计量必须说明口径**：中位数是谁的中位数、样本是什么、带符号还是绝对值。
   典型错误：报「预测误差中位 X」时用了**带符号中位**，正负相互抵消；
   真正该报的是**绝对偏离中位**。两者必须分别列出。
4. **不确定性要前置**，不能藏在文末免责里。
5. **语言分工**：md 文档中文，代码及其注释、docstring、日志与异常消息一律美式英文。
   完整口径见 §2.3，对照表与自查命令见 `/quant-code-standards` §零。
6. **收益数字一律用率呈现**，并同时给出本金口径（见 `/backtest-discipline` §业绩率口径）。
   禁止用区间总额做头条。

### 2.1 Reference 清单（强制，放在输出最末尾）

**凡涉及技术结论、数值、口径判断的输出，末尾必须附 Reference 清单**，把每个决策追溯到具体来源：

| # | 决策 / 结论 | 依据类型 | 具体来源（精确到文件:行号 / 数值 / 接口路径 / 文档 URL） |
|---|---|---|---|
| 1 | … | A 本地 md/代码/配置 ｜ B 本地数据 ｜ C 官方权威源 | … |

- 禁止「根据文档」「根据代码」「根据 OKX 文档」这类**不指名**的表述；
  引官方文档要给到 URL 与章节标题，引接口要给到路径与实际返回片段。
- **没有来源的决策必须显式标注「未证实」**，不允许省略不写
- 验证来源 A/B/C **满足任一条即可**，但选定那条须落到实处

### 2.1.1 开始/结束时间（强制，每次返回结果的首行与末行）

**每一次向用户返回结果：第一行标注开始时间，最后一行标注结束时间**
（结束时间置于 Reference 清单之后，即全文最末）。

- **开始时间 = 本回合用户消息的到达时刻（先于一切思考）**：从会话转录文件
  `~/.claude/projects/-Users-hb-Desktop-quant/<会话ID>.jsonl` 读取最后一条真实用户消息的
  `timestamp`（UTC，转本地时区）。不得用「回合内首个命令现取的 date」当开始时间，
  那已晚于思考开始；仅当转录文件不可读时才以其兜底并注明。
  结束时间 = 回复收尾时刻现取，不得复用回合早前的旧时间戳。
- 格式（两者相同）：`开始时间：YYYY-MM-DD HH:MM:SS` / `结束时间：YYYY-MM-DD HH:MM:SS`。
- 本条仅约束**交互回复**，对文档正文（§2.2）不加此两行。

### 2.1.2 技术文档面向机器读者

本项目产出的技术类 md 文档（方法论、口径裁定、缺陷清单、修复计划、操作卡、
数据说明）的受众是 AI 与程序化读取，不是人类视觉阅读。撰写要求：

- 禁止装饰性符号：不用 emoji 与图形标记（对勾、叉号、警告、禁止、火箭一类），
  不用视觉强调堆砌；风险与状态一律用文字表述（「禁止」「待办」「已验证」「未验证」）。
- 不写渲染性修辞（「好消息」「分毫不差」「杀手锏」一类），不用箭头链替代因果陈述；
  因果与流程用完整句子或编号步骤写清。
- 结构：编号章节 + 表格 + 平实技术陈述，语言风格参照 `.claude/skills/` 下各 SKILL.md。
- 本条约束 md 文档产出；交互回复仍按沟通清晰原则执行，但同样避免符号堆砌。

### 2.2 文档客观性

**本项目产出的一切文档（含研究笔记、裁定、报告）都视为独立留痕件，与交互过程无关。**
撰写时：

- 禁止第二人称称呼读者（「你」「您」「你的任务」），改用客观第三人称或无主语陈述。
- 禁止引用交互过程（「本轮按用户要求」「等你决定」「我们补测即可」）。
- 禁止 AI 自我署名或自述过程（「作者：Claude」「未改动任何文件」这类元信息）。
- 只陈述客观事实、结论、依据与待办。

例外：`research/notes/` 下的个人研究随笔与 `WORKING_MEMORY.md` 不受本条约束，可自由记述。

### 2.3 语言纪律（文档中文 / 代码美式英文）

语言分工固定如下，无例外：

| 载体 | 语言 | 具体范围 |
|---|---|---|
| 纪律与 skills 文档 | 中文 | 本文件、`.claude/skills/*/SKILL.md` |
| 其余项目 md 文档 | 中文 | `ARCHITECTURE.md`、`WORKING_MEMORY.md`、各目录 `README.md`、`DATA_SPEC.md`、`fixplans/`、`research/` 与 `reports/` 下的文档正文 |
| 用户指定的输出 | 按用户当轮指定 | 用户明言要中文的交付物用中文；未指定的按上一行口径 |
| 代码及一切源文件内的文本 | 美式英文 | `.py` `.yaml` `.yml` `.json` `.toml` `.sh` `.sql` 等源文件中的：标识符、注释、docstring、日志消息、异常消息、CLI 帮助与参数说明、测试名、数据列名、状态枚举值、配置键名 |

硬性条款：

1. 代码文件内不出现中文字符，注释与 docstring 同样适用。唯一例外是必须原样保留的
   外部数据（例如交易所返回的中文字段值），此类值只能作为字符串常量出现并注明来源。
   代码内允许出现的非 ASCII 字符只有章节引用符号 `§`（用于引用本文件的条款号）。
2. 拼写取美式不取英式：`canceled` 而非 `cancelled`，`normalize` 而非 `normalise`，
   以及 `analyze`、`color`、`behavior`、`center`、`catalog`、`gray`、`modeling`、`license`。
   同一概念在全项目内只用一种拼写。完整对照表与自查命令见 `/quant-code-standards` §零。
3. 第三方 API 的字段名、参数名、方法名照抄外部拼写，不得改动。那是外部契约。
4. 禁止拼音标识符（`jiage`、`chicang`）与中英混写标识符（`btc_价格`）。
5. 注释与 docstring 写成完整英文句子，首字母大写，句末加句号。
6. md 文档中嵌入的代码块同样受本条约束：示例代码的注释与 docstring 一律英文。
7. 交互回复用中文；回复中引用的代码片段按代码口径，保持英文原样。
8. git commit message 沿用中文，与既有提交历史一致；改动此项须先问用户。

---

## 三、资金红线（本项目最高硬约束，违反即任务失败）

本项目操作的是**真实自有资金**。以下为不可逾越的硬性约束：

### 3.1 下单授权

- **本方绝不主动发出真实委托**。任何会向 OKX 或 Trading 212 提交订单、
  撤单、划转、转账的调用，只允许在**用户当轮明确授权**下执行；
  上一轮的授权不延续到下一轮，一次授权不覆盖后续同类操作。
- **默认 `DRY_RUN = True`**。所有执行层代码必须带 dry-run 开关，
  且默认值为 True；把它改成 False 属于「影响实盘下单行为的参数」，按 §1.4 须先问。
- **禁止本方执行任何提现、划转、法币出入金操作**，包括测试性质的小额操作。
- 只读接口（行情、K 线、账户余额查询、历史成交查询）不在此限，可自由调用。

### 3.2 密钥

- **API Key / Secret / Passphrase 绝不写入代码、配置模板、日志、报告、
  markdown 文档、git 提交或任何会被读取展示的位置。**
- 密钥只允许存放于 `secrets/`（已 gitignore）或环境变量；代码一律通过
  `common/secrets.py` 的读取函数取用，禁止在业务代码里直接读文件。
- 本方不代为申请、创建、修改交易所 API Key，不代为设置其权限位。
  用户自行在交易所后台操作。
- 建议（由用户裁定后执行）：研究与回测阶段的 Key 只开只读权限；
  交易权限的 Key 单独一把、绑定 IP 白名单、关闭提现权限。
- 如果在任何文件中发现疑似真实密钥，**立即停下来告知用户**，不要继续操作该文件。

### 3.3 环境隔离

- 配置分环境：`<venue_dir>/config/<venue>.paper.yaml`（模拟/沙盒）与 `<venue_dir>/config/<venue>.live.yaml`（实盘）。
  由环境变量 `QUANT_ENV` 决定加载哪份，**默认值必须是 `paper`**。
- 实盘配置文件里必须有显式的 `live: true` 字段；执行层在提交订单前断言该字段，
  缺失即拒绝下单。
- 未在模拟或沙盒环境完整跑通并对账一致的策略，禁止接实盘配置。

### 3.4 数据不可变

- `data/*/raw/` 下的原始下载件**只增不改**。清洗、修正、重采样的结果
  一律写到 `curated/`，并留下生成脚本与生成时间。
- 原始件删除或覆盖须先问用户。

---

## 四、目录与命名约定

```
quant/
├── CLAUDE.md              本文件，纪律，always-on
├── ARCHITECTURE.md        模块职责地图
├── WORKING_MEMORY.md      跨会话状态（开工必读，收工必写）
├── .claude/skills/        本项目 skills
├── common/                两条线共用：路径、配置、密钥、日志、网络与限频、存储、指标
├── crypto_trading/        OKX 交易代码：client / ingest / strategy / execution / config
├── trading212/            Trading 212 交易代码：同上五层
├── backtest/              回测，独立于交易代码：engine / okx / t212 / results
├── data/                  全部落地字节。整棵 gitignore，预期迁往外置磁盘
│   ├── binance/{raw,curated}   数据源，非交易场所
│   ├── okx/{raw,curated}
│   ├── t212/{raw,curated}
│   └── reference/         合约规格、费率、交易日历（须注明取自哪个接口、取回时间）
├── fixplans/{t212,crypto}/ 交易代码规格，指向 <venue>/strategy 与 <venue>/execution。入库
├── docs/backtest/        回测框架建设计划（framework / validation）。入库
├── docs/data/<source>/    数据的说明与重建凭据。入库
│   ├── DATA_SPEC.md       字段、单位、时区、已知陷阱
│   ├── MANIFEST.jsonl     每分区一条：坐标、上游 URL、字节数、行数
│   └── GAPS.csv           缺口登记
├── research/{prereg,decisions,notes}
├── reports/  logs/  scripts/  tests/  secrets/
```

| 路径 | 约束 |
|---|---|
| `common/` | 不得含任何场所特有的口径；场所差异一律留在 `crypto_trading/` 与 `trading212/` |
| `<venue>/` | 只装**与交易所打交道**的代码；`client.py` 的 REST/WS 客户端由 ingest 与 execution 共用 |
| `<venue>/strategy/` | 信号的**唯一副本**，纯函数；`backtest/` 与 `execution/` 都 import 它，不得各写一份 |
| `<venue>/config/` | `*.example.yaml` 可入库；`*.live.yaml` / `*.paper.yaml` gitignore；**永不含密钥** |
| `backtest/` | 不碰交易所接口，只读 `data/` 与 `<venue>/strategy/`；结果落 `backtest/results/` |
| `secrets/` | 唯一的密钥落地位置，gitignore，永不展示内容 |
| `data/*/raw/` | 只增不改（§3.4） |
| `data/` | **整棵 gitignore**，不得在其中放说明件；说明与凭据一律进 `docs/data/<source>/` |
| `docs/data/<source>/` | 入库。每个数据源须配 `DATA_SPEC.md`（字段、单位、时区、生成脚本）与 `MANIFEST.jsonl`（重建凭据） |
| `fixplans/` | 只放交易代码规格，顶层只有 `t212/` 与 `crypto/`（§六）。回测框架计划不放这里 |
| `docs/backtest/` | 回测框架建设计划。⛔ 不放交易代码规格 |
| `research/prereg/` | 跑结果**之前**冻结的判定标准，与 `decisions/` 同前缀成对 |
| `logs/` `reports/` `backtest/results/` | gitignore（报告产出件按需单独入库） |

`.gitignore` 中排除 `data/` `logs/` `reports/` 的模式**必须根锚定**（写作 `/data/`）。
未锚定的 `data/` 会匹配任意层级的同名目录，曾因此把 `docs/data/` 一并排除。

### 4.1 命名（硬性）

- **一切文件名与目录名只用 ASCII**：英文小写加下划线或连字符，无例外。
  禁止中文、空格、冒号、其他非 ASCII 字符。文档**内容**可用中文，文件名不行。
- 根目录 md 用全大写下划线（`CLAUDE.md`、`ARCHITECTURE.md`、`WORKING_MEMORY.md`、
  `DATA_SPEC.md`）；代码与其余文件用 `snake_case`。
- 数据文件按数据源各自的实际分区落盘，构造一律经 `common/paths.py`，
  不得在业务代码里自行拼接路径。实际布局见 `ARCHITECTURE.md` §3.3。
- 一次性脚本带日期前缀：`scripts/20260819_ingest_okx_klines.py`。

### 4.2 最小产出与工作区整洁（硬性）

项目目录只存放有长期用途的文件。过程性产物必须在本轮任务结束前处置完毕：
要么升格为正式件并登记，要么删除。不允许以「先放着」为由留存。

1. **最小产出量**：每次核验、验证、回测、诊断只保留支撑结论所必需的最小产出集。
   同一结论不留多份等价产物；同一份报告只留最终一版，草稿与中间版当场删除。
2. **纯过渡文件一律清理**：中间 CSV、调试图、分步导出、探索性脚本的落盘输出、
   压缩包解包残留，在本轮结束前统一删除，不问是否「以后可能用得上」。
   确有复用价值的，升格为正式产出件并在所属目录的 `README.md` 中登记。
3. **未完成的产出件不许留在磁盘上**：要么当轮写完，要么删除。半成品报告、
   截断的 HTML、跑失败留下的空壳文件，一律视为过程件处理。
4. **默认落点**：调试脚本、验证脚本、一次性数据抽样、临时输出一律写到系统临时目录，
   不写进项目目录。确需在项目内运行的，运行完即删。
5. **一次性脚本**放 `scripts/` 且带日期前缀。任务结束时二选一，不做判定即视为任务未完成：
   (a) 确认可复用，在 `ARCHITECTURE.md` 与所属目录 `README.md` 登记职责后保留；
   (b) 不可复用，直接删除。
6. **禁止留在项目目录内的文件**：
   - 系统与工具产物：`.DS_Store`、`__pycache__/`、`*.pyc`、`.pytest_cache/`、`*.tmp`
   - 手工备份副本：`*.bak`、`*_old`、`*_bak`、`*.orig`
   - 版本后缀试验件：`*_v2`、`*_new`、`*_final`、`test1.py`。正式的版本化产出件除外
     （例如 `/html-report` 规定的 `report_template_v2.html`），其保留理由须在
     所属目录的 `README.md` 中写明。
   - 空目录；仅含占位文件（`.gitkeep` 一类）的目录
7. **入库与落地是两件事**：`.gitignore` 只阻止入库，不会让文件从磁盘消失。上述类型
   须同时做到写进 `.gitignore` 且从磁盘删除，只做前者等于放任堆积。
8. **不建空目录**：目录只在确有内容要放时才创建，且创建时同步写 `README.md`（§4.3）。
   需要保留一个暂时为空的目录时，用 `README.md` 本身占位并在其中写明用途，
   不使用 `.gitkeep`。
9. **收尾自检（强制，写进最终输出）**：列出本轮在项目目录内新建或落地的全部文件，
   逐个标注去向，取值为「保留（注明登记位置）」或「已删除」。本轮未新建文件时写明该事实。
10. **删除边界**：本方本轮自己产生的过程件直接删除，不必请示；删除既有文件、
    他方会话的产出、或 `data/*/raw/` 下任何内容，一律先问用户（§3.4）。

### 4.3 目录说明文档（硬性）

**每个目录必须配一份 `README.md`**，说明该目录的职责与其中每个文件存在的必要性。

适用范围与豁免：

| 范围 | 是否需要 `README.md` |
|---|---|
| 项目内一切受版本管理或有长期用途的目录 | 需要 |
| `.git/`、`.venv/`、`__pycache__/`、`.pytest_cache/`、`.claude/worktrees/` | 不需要 |
| `data/` 下的分区目录（`<source>/<layer>/<inst>/...`） | 不需要，由 `docs/data/<source>/DATA_SPEC.md` 统一覆盖 |
| `.claude/skills/<name>/` | 不需要，`SKILL.md` 本身即说明 |
| 项目根目录 | 不需要，由 `ARCHITECTURE.md` 与 `CLAUDE.md` 覆盖 |
| gitignore 的运行时产物目录：`logs/`、`reports/`、`backtest/results/`、`data/`、`secrets/` | 不需要，放进去也不入库。其职责在 `ARCHITECTURE.md` §1 与父目录 `README.md` 中登记，允许其在磁盘上为空 |
| 父目录 `README.md` 已逐个列出其全部文件的子目录 | 不需要，例如 `docs/backtest/framework/`、`fixplans/t212/platform/`、`docs/backtest/validation/` 三者的每个文件均已在 `fixplans/README.md` 中登记。此时另写子目录 `README.md` 属双份维护，反而会失同步 |

固定结构，六节，缺一不可：

1. **职责**：一句话说清本目录装什么、不装什么。
2. **文件清单**：表格，逐个文件给出「文件名 / 作用 / 存在必要性 / 谁在用」。
   存在必要性一栏必须能回答「删掉它会坏掉什么」，答不上来的文件应当删除。
3. **子目录索引**：每个子目录一行，指向其 `README.md`。
4. **依赖关系**：本目录读什么、写什么、被谁 import。
5. **产出与清理**：哪些文件是运行产物、何时可清理、哪些必须保留。
6. **变更记录**：日期加改动，只增不改。

时机（三个硬性节点）：

1. **新建目录时**：与目录同时创建 `README.md`，不允许先建目录后补文档。
2. **读到无 `README.md` 的既有目录时**：当场补齐，不推迟。
3. **改动某目录下任何文件前**：第一件事是读该目录及各级父目录的 `README.md`，
   先于读代码。这一步与 §五 读 `WORKING_MEMORY.md` 并列为开工前置动作。

既有 `README.md` 若已用其他结构写成且信息更充分（例如 `backtest/README.md` 的接入说明），
不必推倒重写，补齐缺失的节即可。文件清单一节是硬性的，任何情况下不得缺。

收工核对（强制）：本轮改动过的每一个目录，逐个复核其 `README.md`——
文件增删是否已反映、职责表述是否仍成立、是否有该清理未清理的产物。
核对结果写进最终输出。

### 4.4 脚本头部注释（硬性）

每个源文件的模块头 docstring 是该文件的说明文档，作用等同于 §4.3 的目录 `README.md`。
一切 `.py` 文件（含 `__init__.py`）必须有，内容用美式英文（§2.3）。

固定小节：

1. 一句话职责（Responsibility）。
2. 不负责什么（Out of scope），指向承担该职责的文件。
3. 对外函数与类逐项一行（Public functions / Public classes），名称加一句作用。
4. 关键参数与常量（Parameters / Constants）：名称、单位、取值依据出处。
5. 输入与输出（Inputs / Outputs）：读哪些路径或接口、写哪些路径。
6. 变更历史（Change log）：日期、改了什么、为什么。只增不改。

时机：

1. **改代码前第一件事读头部注释**，先于读函数体。
2. **改完后核对头部注释与实现是否一致**，不一致即为缺陷，当场修正。
3. **借核对做一次精简判断**：不再被调用的函数、无人使用的参数、重复实现的逻辑，
   一律删除而不是留着。删除结果写进变更历史。

模板与逐条细则见 `/quant-code-standards` §3.1。

## 五、跨会话工作记忆（强制）

会话之间上下文不共享。**唯一的跨会话状态载体是项目根目录的 `WORKING_MEMORY.md`。**

- **读的时机（硬性）**：**任何任务开工前先读完 `WORKING_MEMORY.md`**，不得凭上一轮记忆
  或本会话既有印象开工。这一步先于调 skill、先于读计划件。
- **写的时机（硬性）**：任务结束前更新——刷新「当前状态」与「未决项」，
  在「时间线」**追加**一行（时刻 + 动作）。时间线只增不改；结论变了写新行注明
  「原值改为现值，并注明依据」，不删旧行。
- **内容边界**：只记状态、裁定、未决、时间线；正文结论仍写进各自的记录件
  （研究笔记、裁定、报告），工作记忆只指路，避免双份维护而失同步。
- 本机制不替代 `~/.claude/projects/-Users-hb-Desktop-quant/memory/` 的本方私有记忆：
  私有记忆存长期偏好与稳定事实，`WORKING_MEMORY.md` 存本项目的当前工作状态。二者不互相复制。

---

## 六、既有工作约定

- 两条线的代码、配置、数据、结论一律隔离；共用逻辑才下沉到 `common/`。
- 代码改动**直接做到底**，不要中途反复请示（但 §1 的「未证实即停」与 §3 的资金红线
  优先级更高）。
- 涉及交易所 API 的任何编码，先按 §1.1 的 S4 取回权威事实并落到
  `data/reference/` 或代码注释，再动手。
- 回测口径以 `research/decisions/` 下最新一份裁定为准，不是脚本内的默认值。
- **`fixplans/` 只放交易代码的规格**，且每份说明都必须指向具体的交易代码文件
  （含策略层）。`trading212/` 与 `crypto_trading/` 会读取该目录来更新策略与执行代码，
  因此规格里的每一条都要能对应到一个可改的文件与函数，不得只写方法论。
  目录只有两级顶层：`fixplans/t212/` 与 `fixplans/crypto/`，其下按策略或主题分目录
  （如 `fixplans/t212/a0/`）。⛔ 不得在 `fixplans/` 下新开其他顶层目录。
  回测框架自身的建设计划不属交易代码，放 `docs/backtest/`。
- 本项目**不做**、也不接受以下内容：给他人的投资建议、代客理财、
  任何形式的对外发布荐股/荐币。产出只服务于用户本人的自有资金决策。
