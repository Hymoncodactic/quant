# 项目规则（always-on，不需调用即生效）

> 本文件是 **验证纪律**，对本项目内所有对话、所有任务无条件生效。
> 代码开发的**完整流程**另见 skill：`/verified-dev`。
>
> **本项目与 `WORK FILES/` 工作区完全独立**：两边的规则、记忆、skills、数据互不引用、
> 互不复制。本项目的私有记忆在 `~/.claude/projects/-Users-hb-Desktop-quant/memory/`，
> 与工作区记忆目录物理隔离。⛔ 禁止把工作区的标的、口径、结论、路径带入本项目，
> 反之亦然。

---

## 零、项目定位（一句话）

个人自有资金的量化交易项目，两条并行方向：

| 方向 | 场所 | 标的范围 | 计价货币 |
|---|---|---|---|
| 加密货币 | OKX | 主流高流动性币种（BTC、ETH 等） | USDT 计价，见 `crypto_trading/config/` |
| 股票 | Trading 212 | 高流动性股票 | GBP（账户内存放的是英镑） |

两条线共用一套纪律（本文件）、一套 skills 与 `common/` 基础库，
但**数据、配置、风控参数、回测结论一律分场所隔离**，不得互相套用。

---

## 一、推断纪律（最高优先级）

### 1.1 什么算「已证实」

一个判断只有满足以下**任一条**，才可以当作事实使用：

| # | 证实来源 | 本项目的举例 |
|---|---|---|
| S1 | **本地代码**：能指出确切文件与行号 | `MAKER_FEE_BPS = 8`（`crypto_trading/config/fees.py` L12） |
| S2 | **本地生效配置**：实际被加载的那一份 | `crypto_trading/config/okx.live.yaml`（由 `QUANT_ENV` 决定加载哪份） |
| S3 | **本地说明文档**：且已与代码交叉比对无冲突 | `data/okx/DATA_SPEC.md` 的字段定义 ∧ 代码实际读取一致 |
| S4 | **权威外部源**：交易所/券商官方文档、官方接口返回值 | OKX API 官方文档的字段说明、`GET /api/v5/public/instruments` 的实际返回 |
| S5 | **数据实证**：可复现的量化检验，且该检验有判别力 | 用实际下载的 K 线验证时间戳是开盘时刻还是收盘时刻 |
| S6 | **用户在 prompt 中明言的要求或事实** | 「Trading212 里面我存放的是英镑」 |

**除此之外的一切，都是推断（inference），不是事实。**

⚠️ **本项目的特别提醒**：交易所 API 的字段含义、单位、精度、费率、限频、
最小下单量——**一律不得凭模型记忆作答**。这些是 S4 类事实，必须现查官方文档
或现调接口取回实际返回值，并在 Reference 清单中给出文档 URL / 接口路径 / 返回样本。
凭记忆写出的 API 参数是本项目最高频的错误来源，且错误直接对应真金白银的损失。

### 1.2 遇到未证实的假设 —— **停下来问，不要静默推断**

只要某个判断达不到上面任一条，**必须停止，向用户提问**。不得：

- ❌ 默默按最可能的猜测继续做下去
- ❌ 把反推（用结果倒推输入）包装成「已校验」「已验证」
- ❌ 只在文档角落写一句免责，正文却当事实用

提问时必须给出：**① 我的推断是什么 ② 依据是什么、强度如何 ③ 怎样才能真正证实
（具体到查哪个文件/哪个官方文档章节/哪个接口字段）④ 如果推断错了会有什么后果。**

### 1.3 用户 prompt 中的明言（S6）的特殊处理

- 用户明言的要求/事实 → **可直接当作已证实的假设采用**，不必反复求证。
- **但如果它与代码事实、文档记载或我的既有认知相矛盾** → **必须停下来向用户核实**，不得：
  - ❌ 默默按用户说的做（可能用户口误或记错）
  - ❌ 默默按代码事实做（违背用户意图）
  - ✅ 正确：摆出冲突双方的具体证据，请用户裁定

### 1.4 关键决策归用户

以下情形一律先问，不自行拍板：

- 会**改变交付物中的数字**的口径变更
- 涉及**外部事实**（交易所规则、费率档位、账户实情、可用资金额度、税务口径）
- **写死在代码里的常量**，其取值依据不在本地
- 结论方向的**定性表述**（如「保守下界」vs「乐观上界」）
- **任何影响实盘下单行为的参数**（阈值、仓位上限、频率上限、止损位）

---

## 二、表述纪律

1. **区分「跑通」与「正确」**：程序无报错 ≠ 逻辑正确。二者必须分别陈述。
2. **数字必须带出处**：引用任何数值，注明来自哪个文件/行号/命令/接口返回。
3. **统计量必须说明口径**：中位数是谁的中位数、样本是什么、带符号还是绝对值。
   > 反面教材：报「预测误差中位 X」时用了**带符号中位**（正负抵消的假象）；
   > 真正该报的是**绝对偏离中位**。两者必须分别列出。
4. **不确定性要前置**，不能藏在文末免责里。
5. **收益数字一律用率呈现**，并同时给出本金口径（见 `/backtest-discipline` §业绩率口径）。
   禁止用区间总额做头条。

### 2.1 Reference 清单（强制，放在输出最末尾）

**凡涉及技术结论、数值、口径判断的输出，末尾必须附 Reference 清单**，把每个决策追溯到具体来源：

| # | 决策 / 结论 | 依据类型 | 具体来源（精确到文件:行号 / 数值 / 接口路径 / 文档 URL） |
|---|---|---|---|
| 1 | … | A 本地 md/代码/配置 ｜ B 本地数据 ｜ C 官方权威源 | … |

- 禁止「根据文档」「根据代码」「根据 OKX 文档」这类**不指名**的表述；
  引官方文档要给到 URL 与章节标题，引接口要给到路径与实际返回片段。
- **没有来源的决策必须显式标 ⚠️ 未证实**，不允许省略不写
- 验证来源 A/B/C **满足任一条即可**，但选定那条须落到实处

### 2.1.1 开始/结束时间（强制，每次返回结果的首行与末行）

**每一次向用户返回结果：第一行标注开始时间，最后一行标注结束时间**
（结束时间置于 Reference 清单之后，即全文最末）。

- **开始时间 = 本回合用户消息的到达时刻（先于一切思考）**：从会话转录文件
  `~/.claude/projects/-Users-hb-Desktop-quant/<会话ID>.jsonl` 读取最后一条真实用户消息的
  `timestamp`（UTC，转本地时区）。⛔ 不得用「回合内首个命令现取的 date」当开始时间
  ——那已晚于思考开始；仅当转录文件不可读时才以其兜底并注明。
  结束时间 = 回复收尾时刻现取，不得复用回合早前的旧时间戳。
- 格式（两者相同）：`开始时间：YYYY-MM-DD HH:MM:SS` / `结束时间：YYYY-MM-DD HH:MM:SS`。
- 本条仅约束**交互回复**，对文档正文（§2.2）不加此两行。

### 2.1.2 技术文档面向机器读者

本项目产出的技术类 md 文档（方法论、口径裁定、缺陷清单、修复计划、操作卡、
数据说明）的受众是 AI 与程序化读取，不是人类视觉阅读。撰写要求：

- 禁止装饰性符号：不用 emoji（含 ✅ ❌ ⚠️ ⛔ 🚀 等）、不用视觉强调堆砌；
  风险与状态用文字表述（「禁止」「待办」「已验证」「未验证」）。
- 不写渲染性修辞（「好消息」「分毫不差」「杀手锏」一类），不用箭头链替代因果陈述；
  因果与流程用完整句子或编号步骤写清。
- 结构：编号章节 + 表格 + 平实技术陈述，语言风格参照 `.claude/skills/` 下各 SKILL.md。
- 本条约束 md 文档产出；交互回复仍按沟通清晰原则执行，但同样避免符号堆砌。

### 2.2 文档客观性

**本项目产出的一切文档（含研究笔记、裁定、报告）都视为独立留痕件，与交互过程无关。**
撰写时：

- ❌ 禁止第二人称称呼读者（「你」「您」「你的任务」），改用客观第三人称或无主语陈述。
- ❌ 禁止引用交互过程（「本轮按用户要求」「等你决定」「我们补测即可」）。
- ❌ 禁止 AI 自我署名/自述过程（「作者：Claude」「未改动任何文件」这类元信息）。
- ✅ 只陈述客观事实、结论、依据与待办。

例外：`research/notes/` 下的个人研究随笔与 `WORKING_MEMORY.md` 不受本条约束，可自由记述。

---

## 三、资金红线（本项目最高硬约束，违反即任务失败）

本项目操作的是**真实自有资金**。以下为不可逾越的硬性约束：

### 3.1 下单授权

- ⛔ **本方绝不主动发出真实委托**。任何会向 OKX 或 Trading 212 提交订单、
  撤单、划转、转账的调用，只允许在**用户当轮明确授权**下执行；
  上一轮的授权不延续到下一轮，一次授权不覆盖后续同类操作。
- ⛔ **默认 `DRY_RUN = True`**。所有执行层代码必须带 dry-run 开关，
  且默认值为 True；把它改成 False 属于「影响实盘下单行为的参数」，按 §1.4 须先问。
- ⛔ **禁止本方执行任何提现、划转、法币出入金操作**，包括测试性质的小额操作。
- ⚠️ 只读接口（行情、K 线、账户余额查询、历史成交查询）不在此限，可自由调用。

### 3.2 密钥

- ⛔ **API Key / Secret / Passphrase 绝不写入代码、配置模板、日志、报告、
  markdown 文档、git 提交或任何会被读取展示的位置。**
- 密钥只允许存放于 `secrets/`（已 gitignore）或环境变量；代码一律通过
  `common/secrets.py` 的读取函数取用，禁止在业务代码里直接读文件。
- ⛔ 本方不代为申请、创建、修改交易所 API Key，不代为设置其权限位。
  用户自行在交易所后台操作。
- ⚠️ 建议（由用户裁定后执行）：研究/回测阶段的 Key 只开只读权限；
  交易权限的 Key 单独一把、绑定 IP 白名单、关闭提现权限。
- 如果在任何文件中发现疑似真实密钥，**立即停下来告知用户**，不要继续操作该文件。

### 3.3 环境隔离

- 配置分环境：`<venue_dir>/config/<venue>.paper.yaml`（模拟/沙盒）与 `<venue_dir>/config/<venue>.live.yaml`（实盘）。
  由环境变量 `QUANT_ENV` 决定加载哪份，**默认值必须是 `paper`**。
- 实盘配置文件里必须有显式的 `live: true` 字段；执行层在提交订单前断言该字段，
  缺失即拒绝下单。
- ⛔ 未在模拟/沙盒环境完整跑通并对账一致的策略，禁止接实盘配置。

### 3.4 数据不可变

- ⛔ `data/*/raw/` 下的原始下载件**只增不改**。清洗、修正、重采样的结果
  一律写到 `curated/`，并留下生成脚本与生成时间。
- 原始件删除或覆盖须先问用户。

---

## 四、目录与命名约定

```
quant/
├── CLAUDE.md              本文件，纪律，always-on
├── ARCHITECTURE.md        模块职责地图
├── WORKING_MEMORY.md      跨会话状态（开工必读，收工必写）
├── .claude/skills/        本项目 skills
├── common/                两条线共用：路径、配置、密钥、日志、网络与限频、存储、指标
├── crypto_trading/        OKX 交易代码：client / ingest / strategy / execution / config
├── trading212/            Trading 212 交易代码：同上五层
├── backtest/              回测，独立于交易代码：engine / okx / t212 / results
├── data/                  全部落地数据
│   ├── okx/{raw,curated}
│   ├── t212/{raw,curated}
│   └── reference/         合约规格、费率、交易日历（须注明取自哪个接口、取回时间）
├── research/{prereg,decisions,notes}
├── reports/  logs/  scripts/  tests/  secrets/
```

| 路径 | 约束 |
|---|---|
| `common/` | 不得含任何场所特有的口径；场所差异一律留在 `crypto_trading/` 与 `trading212/` |
| `<venue>/` | 只装**与交易所打交道**的代码；`client.py` 的 REST/WS 客户端由 ingest 与 execution 共用 |
| `<venue>/strategy/` | 信号的**唯一副本**，纯函数；`backtest/` 与 `execution/` 都 import 它，⛔ 不得各写一份 |
| `<venue>/config/` | `*.example.yaml` 可入库；`*.live.yaml` / `*.paper.yaml` gitignore；**永不含密钥** |
| `backtest/` | 不碰交易所接口，只读 `data/` 与 `<venue>/strategy/`；结果落 `backtest/results/` |
| `secrets/` | 唯一的密钥落地位置，gitignore，永不展示内容 |
| `data/*/raw/` | 只增不改（§3.4） |
| `data/*/curated/` | 每个目录须配 `DATA_SPEC.md`，写明字段、单位、时区、生成脚本 |
| `research/prereg/` | 跑结果**之前**冻结的判定标准，与 `decisions/` 同前缀成对 |
| `logs/` `reports/` `backtest/results/` | gitignore（报告产出件按需单独入库） |

### 4.1 命名（硬性）

- ⛔ **一切文件名与目录名只用 ASCII**：英文小写 + 下划线/连字符，无例外。
  禁止中文、空格、冒号、其他非 ASCII 字符。文档**内容**可用中文，文件名不行。
- 根目录 md 用全大写下划线（`CLAUDE.md`、`ARCHITECTURE.md`、`WORKING_MEMORY.md`、
  `DATA_SPEC.md`）；代码与其余文件用 `snake_case`。
- 数据文件按 `data/<venue>/<layer>/<instrument>/<period>/year=YYYY/` 分区，
  文件名 `YYYYMMDD.parquet`。
- 一次性脚本带日期前缀：`scripts/20260819_ingest_okx_klines.py`。

## 五、跨会话工作记忆（强制）

会话之间上下文不共享。**唯一的跨会话状态载体是项目根目录的 `WORKING_MEMORY.md`。**

- **读的时机（硬性）**：**任何任务开工前先读完 `WORKING_MEMORY.md`**，不得凭上一轮记忆
  或本会话既有印象开工。这一步先于调 skill、先于读计划件。
- **写的时机（硬性）**：任务结束前更新——刷新「当前状态」与「未决项」，
  在「时间线」**追加**一行（时刻 + 动作）。时间线只增不改；结论变了写新行注明
  「原值 → 现值 + 依据」，不删旧行。
- **内容边界**：只记状态、裁定、未决、时间线；正文结论仍写进各自的记录件
  （研究笔记、裁定、报告），工作记忆只指路，避免双份维护而失同步。
- 本机制不替代 `~/.claude/projects/-Users-hb-Desktop-quant/memory/` 的本方私有记忆：
  私有记忆存长期偏好与稳定事实，`WORKING_MEMORY.md` 存本项目的当前工作状态。二者不互相复制。

---

## 六、既有工作约定

- 两条线的代码、配置、数据、结论一律隔离；共用逻辑才下沉到 `common/`。
- 代码改动**直接做到底**，不要中途反复请示（但 §1 的「未证实即停」与 §3 的资金红线
  优先级更高）。
- 涉及交易所 API 的任何编码，先按 §1.1 的 S4 取回权威事实并落到
  `data/reference/` 或代码注释，再动手。
- 回测口径以 `research/decisions/` 下最新一份裁定为准，不是脚本内的默认值。
- 本项目**不做**、也不接受以下内容：给他人的投资建议、代客理财、
  任何形式的对外发布荐股/荐币。产出只服务于用户本人的自有资金决策。
